# Skill Creator 设计方案

## 1. 产品目标

开发一个以业务需求为起点的 Skill Creator 名称为 cute-skill-creator：先引导用户澄清业务，再根据业务流程为每个节点选择合适的执行方式，生成可执行的 Skill，完成测试验证并输出可追溯的测试报告。

核心流程：

```text
业务澄清 → 流程分析与执行方式选择 → 生成 Skill → 测试验证 → 修订与重测 → 交付
```

核心交付物：

- 业务需求说明。
- 带执行方式及选择依据的流程设计。
- 可执行的 Skill 及必要的脚本、参考资料。
- 测试报告及对应证据。

建议第一版采用 Creator Skill 形式验证工作流，再根据需要封装为命令行或网页工具。产品形态尚待确定。

## 2. 业务澄清

### 2.1 澄清方式

复用用户已经提供的信息，先汇总全部已知待决事项，再将问题逐项编号，以菜单选项一次性向用户提问。优先使用宿主支持的批量菜单工具；工具不可用或题数超过上限时，一次展示完整文本菜单，支持按题号选择和自定义答案。

用户统一回复即确认对应事项，信息充分后直接进入流程设计、生成和验证，无需再次确认摘要或逐步批准继续。仅对关键漏答、答案冲突或新出现的关键依赖集中补问；非关键细节记录默认假设。澄清以信息充分为结束条件，不以固定问题数量或对话轮数为结束条件。具体交互规则见 [菜单式集中澄清](skills/cute-skill-creator/references/clarification.md)。

| 澄清内容 | 要解决的问题 |
| --- | --- |
| 业务目标 | 谁使用？解决什么问题？怎样算成功？ |
| 输入与输出 | 接收什么数据或文件？最终交付什么？ |
| 业务流程 | 主要步骤、分支、异常及人工介入点是什么？ |
| 规则与判断 | 哪些有明确公式或规则？哪些依赖经验判断？ |
| 环境与权限 | 有哪些工具、数据源？哪些操作允许自动执行？ |
| 验收案例 | 正常、边界和失败场景下，分别期望什么结果？ |

### 2.2 需求状态

Creator 持续维护需求说明，将信息标记为：

- **已确认**：用户明确提供或确认的信息。
- **待确认**：仍存在缺失或歧义的信息。
- **暂定假设**：为继续设计而采用、尚未获得确认的假设。

影响执行正确性、权限或验收的关键信息缺失时，继续澄清，不进入依赖这些信息的生成或执行步骤。非关键细节可以采用明确记录的默认值。

### 2.3 信息充分标准

当每个核心步骤都能够定义以下内容时，可以进入生成阶段：

- 输入。
- 预期输出。
- 业务约束。
- 验收方法。

## 3. 流程分析与执行方式选择

### 3.1 按节点分类

Creator 根据业务需求拆解流程，自动识别每个节点适合采用的执行方式，并记录选择依据。

一个 Skill 可以同时包含三种执行方式，不必整体归为某一类。

| 执行方式 | 适用情况 | 实现方式 | 示例 |
| --- | --- | --- | --- |
| AI 自主推理 | 输入开放、存在歧义、合理答案不唯一 | 明确目标、证据要求和质量标准，保留推理空间 | 归纳客户痛点、解释异常、提出方案 |
| 精确代码执行 | 规则明确、结果可计算、要求可重复 | 用脚本实现计算、转换和校验 | 金额计算、字段映射、去重、文件导出 |
| 半自主执行 | 需要语义判断，同时存在严格约束 | AI 生成结构化候选，代码检查并执行；必要时人工决定 | AI 推荐字段映射，代码验证后转换数据 |

### 3.2 半自主执行与人工批准

半自主执行不意味着每步都需要询问用户。它可以采用“AI 决策 + 代码约束”的方式。

是否需要人工批准，由业务权限和操作影响单独决定，不由执行方式直接决定。

### 3.3 节点定义

每个流程节点至少记录：

```text
输入 → 执行方式及理由 → 输出
约束 → 校验方法 → 失败处理 → 人工介入条件
```

当一个节点同时包含判断和计算时，进一步拆分。例如，“分析利润”可拆分为：

1. 代码计算利润指标。
2. AI 根据计算结果和业务证据解释变化原因。

## 4. Skill 生成

### 4.1 生成内容

生成内容与业务流程及节点定义对应：

| 资源 | 内容 |
| --- | --- |
| `SKILL.md` | 触发条件、核心流程、节点衔接和异常处理 |
| `scripts/` | 需要确定性执行的代码 |
| `references/` | 业务规则、数据定义和判断依据 |
| 测试用例 | 输入、预期结果或评分标准、覆盖的节点与分支 |

### 4.2 组织原则

- 目录和资源按实际需要生成。
- 核心指令保持精简，详细资料按需引用。
- 测试用例与验收要求对应。
- 测试运行记录放在独立工作目录，避免每次使用 Skill 都加载测试材料。

## 5. 测试验证

### 5.1 测试范围

测试覆盖结构、触发、节点、混合交接及异常回归。

| 测试层次 | 验证内容 |
| --- | --- |
| 结构与依赖 | 格式正确、引用有效、脚本可运行、依赖可用 |
| 触发测试 | 应触发的请求是否触发，相似但无关的请求是否被排除 |
| 节点测试 | 代码结果是否准确；AI 判断是否满足证据与质量标准 |
| 混合执行测试 | AI 输出不合法、缺字段或违反规则时，代码能否正确处理 |
| 异常与回归测试 | 缺失数据、工具失败、边界条件，以及修订后已有能力是否退化 |

### 5.2 不同执行方式的验证方法

- **代码执行**：尽量使用确定性断言，验证计算、转换和规则执行结果。
- **AI 自主推理**：使用明确的评分标准和证据，必要时由人工评审；通过重复运行检查稳定性。
- **半自主执行**：验证 AI 与代码的交接，以及不合法候选、校验失败和人工介入路径。

对于 AI 节点，使用无 Skill 或旧版 Skill 作为对照，评估新增或修订后的 Skill 是否改善任务结果。

### 5.3 修订与重测

依据测试发现的问题修订 Skill，并重新执行受影响的测试及必要的回归测试。记录问题、修改内容和重测结果，保留对应日志与产物。

## 6. 测试报告

### 6.1 必需内容

```text
测试对象：Skill 版本、模型、运行环境
测试范围：覆盖的业务需求、节点与分支
测试结果：通过 / 失败 / 阻塞 / 未执行
逐项证据：输入、预期、实际结果、日志或产物
质量指标：触发准确性、执行覆盖率、耗时、Token 用量
修订记录：发现的问题、修改内容、重测结果
未解决项：原因、影响、下一步处理方式
```

### 6.2 结果状态

| 状态 | 含义 |
| --- | --- |
| 通过 | 已实际执行，且结果满足对应验收标准 |
| 失败 | 已实际执行，但结果未满足对应验收标准 |
| 阻塞 | 因环境、依赖、数据或权限等条件不足，无法完成测试 |
| 未执行 | 已列入测试范围，但尚未实际执行 |

### 6.3 验证通过条件

只有约定的必测项全部执行并通过，才能标记“验证通过”。

没有真实执行条件的测试必须标为阻塞或未执行，不得计为通过。

以下状态必须分别呈现：

- 已生成测试用例。
- 静态检查通过。
- 业务效果验证通过。

## 7. 最终交付

最终交付应让用户能够理解业务需求如何转化为执行流程，并根据测试证据判断 Skill 是否可用。

| 交付物 | 目的 |
| --- | --- |
| 业务需求说明 | 记录目标、范围、输入输出、约束和验收标准 |
| 流程设计 | 展示节点、执行方式、选择依据、衔接及异常路径 |
| 可执行 Skill | 提供核心指令及必要的代码、参考资料 |
| 测试报告与证据 | 说明已验证内容、实际结果和未解决问题 |

本文件为产品设计方案，不代表已经实现 Creator 或完成其测试。
